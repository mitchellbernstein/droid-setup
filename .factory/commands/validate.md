---
description: Run the repo's validators and record results in the handoff.
---

Validate current changes.

- Use the `validate-changes` skill.
- Prefer quick checks first (lint/typecheck), then tests.
- Record commands/results under **Validation** in `thoughts/handoffs/latest.md`.
